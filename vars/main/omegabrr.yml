---

################################
# BASICS
################################

autobrr_omegabrr_is_enabled: '{{ (autobrr_omegabrr_basics_name is defined) and
                                 (autobrr_omegabrr_basics_name is not none) and
                                 (autobrr_omegabrr_basics_name | trim | length > 0) and
                                 (autobrr_omegabrr_basics_image_repo is defined) and
                                 (autobrr_omegabrr_basics_image_repo is not none) and
                                 (autobrr_omegabrr_basics_image_repo | trim | length > 0) and
                                 (autobrr_omegabrr_basics_image_tag is defined) and
                                 (autobrr_omegabrr_basics_image_tag is not none) and
                                 (autobrr_omegabrr_basics_image_tag | trim | length > 0) and
                                 (autobrr_omegabrr_basics_restart_policy is defined) and
                                 (autobrr_omegabrr_basics_restart_policy is not none) and
                                 (autobrr_omegabrr_basics_restart_policy | trim | length > 0) and
                                 (autobrr_omegabrr_env_timezone is defined) and
                                 (autobrr_omegabrr_env_timezone is not none) and
                                 (autobrr_omegabrr_env_timezone | trim | length > 0) and
                                 (autobrr_omegabrr_env_puid is defined) and
                                 (autobrr_omegabrr_env_puid is not none) and
                                 (autobrr_omegabrr_env_puid | trim | length > 0) and
                                 (autobrr_omegabrr_env_pgid is defined) and
                                 (autobrr_omegabrr_env_pgid is not none) and
                                 (autobrr_omegabrr_env_pgid | trim | length > 0) and
                                 (autobrr_omegabrr_ports_host is defined) and
                                 (autobrr_omegabrr_ports_host is not none) and
                                 (autobrr_omegabrr_ports_host | trim | length > 0) and
                                 (autobrr_omegabrr_ports_cont is defined) and
                                 (autobrr_omegabrr_ports_cont is not none) and
                                 (autobrr_omegabrr_ports_cont | trim | length > 0) and
                                 (autobrr_omegabrr_paths_folder is defined) and
                                 (autobrr_omegabrr_paths_folder is not none) and
                                 (autobrr_omegabrr_paths_folder | trim | length > 0) and
                                 (autobrr_omegabrr_paths_location is defined) and
                                 (autobrr_omegabrr_paths_location is not none) and
                                 (autobrr_omegabrr_paths_location | trim | length > 0) and
                                 (autobrr_omegabrr_binds_config_mapping is defined) and
                                 (autobrr_omegabrr_binds_config_mapping is not none) and
                                 (autobrr_omegabrr_binds_config_mapping | trim | length > 0) }}'